
name: turntaking-workflow
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
      - snakemake
      - pyyaml
      - numpy
      - scipy
      - mne
      - pandas
      # Install the project itself into the env (editable)
      - -e ../..